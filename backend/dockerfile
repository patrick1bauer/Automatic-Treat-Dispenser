FROM python:3.11-slim

WORKDIR /app

# The app uses pigpio via the daemon, so no extra GPIO package is needed here.
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Copy your app code
COPY . .

EXPOSE 61002
CMD ["python", "app.py"]